services:
  # Backend API Service only (Frontend on Netlify)
  - type: web
    name: portfolio-backend
    env: node
    buildCommand: "npm install"
    startCommand: "node simple-blog-server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
